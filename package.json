{
  "name": "fundacion",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:cms": "pnpm --filter cms dev",
    "build:web": "pnpm --filter web build",
    "build:cms": "pnpm --filter cms build",
    "start:cms": "pnpm --filter cms start",
    "types:generate": "pnpm --filter cms generate:types",
    "dev": "pnpm dev:cms & pnpm dev:web"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "workerd", "@parcel/watcher"]
  }
}
